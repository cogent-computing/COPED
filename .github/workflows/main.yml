# Basic workflow to spin up the application and test the COPED web service is responding.

name: coped-test-runner

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

  # Allows the workflow to be run manually from the Actions tab
  workflow_dispatch:

jobs:
  spin-up:
    runs-on: self-hosted
    steps:
      # Check out the repository under $GITHUB_WORKSPACE
      - uses: actions/checkout@v2

      - name: Set docker project name
        run: echo "COMPOSE_PROJECT_NAME=coped-ci-`date +%s`" >> $GITHUB_ENV

      - name: Start services
        run: |
          cp .env .env.ci
          docker-compose -p "${{ env.COMPOSE_PROJECT_NAME }}" --env-file .env.ci up -d --build
          sleep 30

      - name: Test the web service responds to external requests
        run: curl -I http://127.0.0.1:1337/admin/ | grep "302 Found"

      - name: Test the web service can run CRUD actions on elasticsearch
        run: docker exec coped_web python ./manage.py test

      - name: Tidy up
        run: docker-compose -p "${{ env.COMPOSE_PROJECT_NAME }}" --env-file .env.ci down -v
